# Minimal example
# actions & authentication are inherited from the default profile
# no proxy connection or virtualization support
# truly minimal
id: "host1.example.com"
description: "an example server" # Technically you can still remove this line but it will look weird
connectionSocket: "host1.example.com:22"
